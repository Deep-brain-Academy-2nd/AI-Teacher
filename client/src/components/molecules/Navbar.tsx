import React from 'react';
import { Button, Flex, Image } from 'rebass';
import styled from 'styled-components';

const Navbar = () => {
  return <Flex justifyContent={'center'}>NavBar</Flex>;
};

export default Navbar;
